language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - hhvm

before_script:
  - /home/travis/.phpenv/versions/5.5/bin/composer install --prefer-dist

script: phpunit

matrix:
  allow_failures:
    - php: 5.6
    - php: hhvm
  fast_finish: true

sudo: true

notifications:
    on_success: never
    on_failure: always
